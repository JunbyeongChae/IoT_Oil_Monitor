// secrets.h.example
// 이 파일을 secrets.h로 복사한 뒤 실제 값을 채워 넣으세요.
// secrets.h는 .gitignore에 등록되어 있어 커밋되지 않습니다.

#ifndef SECRETS_H
#define SECRETS_H

// Wi-Fi 접속 정보 (ESP-NOW 채널을 1호기와 맞추기 위해 필요)
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"

#endif
